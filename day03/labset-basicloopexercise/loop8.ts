// const input = process.argv[2];   // รับข้อความจาก CLI เช่น "hello"

// let reversed = "";

// for (let i = input.length - 1; i >= 0; i--) {
//   reversed += input[i];          // ต่อทีละตัวจากท้าย → หน้า
// }

// console.log(reversed);